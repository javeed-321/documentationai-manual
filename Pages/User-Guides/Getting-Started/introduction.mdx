<ExpandableGroup>
  <Expandable title="How does editing policy variables impact policy status?" default-open="false">
    Changing a policy variable affects all policies that utilize it and changes their status to **Draft**.

    | Old status   | New status after updating the Policy Variable |
    | ------------ | --------------------------------------------- |
    | Not Uploaded | Draft                                         |
    | Draft        | Draft                                         |
    | Approved     | Draft                                         |
    | Published    | Draft                                         |
  </Expandable>

  <Expandable title="Can I define a unique value for the variable for just one policy?" default-open="false">
    No. You cannot define a unique value for a variable for just one policy; it applies to all impacted policies. To use a specific value for a policy, delete the variable and type in the specific answer, wherever needed.
  </Expandable>

  <Expandable title="How do I identify the policies that use a specific variable?" default-open="false">
    The **Impacted Policies** section lists all policies that use a specific variable. To check this,

    Navigate to the **Policy** module section and click the **Settings** icon on the top right. Click the **Policy Variables** tab. Click **View impacted policies** to view the list of all policies that use a specific variable.

    <Image src="/images/settings.png" alt="settings.png" />

    <Image src="/images/Screenshot-2025-06-16-at-17.51.54.png" alt="" />
  </Expandable>

  <Expandable title="Why is the variable’s value not visible in the policy editor?" default-open="false">
    The policy editor shows only the placeholder variable. However, the variable value is visible when you preview the policy or download it.
  </Expandable>

  <Expandable title="I’ve accidentally deleted a variable from a policy. What do I do?" default-open="false">
    No worries. Click the **Copy** **icon** in the **Policy Variables** tab to paste the variable back into the policy.

    <Image src="/images/Copy-icon.png" alt="Copy icon.png" />

    <Image src="/images/Screenshot-2025-04-09-at-19.31.56.png" alt="" />

    If you’ve removed all variables from the policy, you can restore them by clicking **Revert To Original Policy**.

    <Image src="/images/Screenshot-2025-04-09-at-19.36.48.png" alt="" />
  </Expandable>

  <Expandable title="Can I add a policy variable directly into the policy?" default-open="false">
    Yes. Click the **Copy** **icon** corresponding to the variable you want to use and paste it at the required place in the policy.

    <Image src="/images/Copy-icon.png" alt="Copy icon.png" />

    <Image src="/images/Screenshot-2025-05-29-at-13.58.13.png" alt="" />
  </Expandable>

  <Expandable title="Will enabling policy variables impact existing policies in my Scrut account?" default-open="true">
    Refer to this section to learn [how enabling policy variables will affect existing policies](/docs/policy/editing-policy-content/policy-variables#how-do-policy-variables-affect-existing-policies).
  </Expandable>

  <Expandable title="Can I add custom variables?" default-open="true">
    You cannot add custom policy variables; only the default options are available.
  </Expandable>

  <Expandable title="Can I use nested policy variables (one policy variable inside another variable's value)?" default-open="true">
    Scrut doesn't currently support nested policy variables. If a variable's value contains a reference to another variable, the inner variable is not resolved in the generated policy document. It appears as plain text instead.

    For example, say you have two variables:

    Variable A: `{{Legal_Entities}}` with the value "Acme Ltd." Variable B: `{{Company_Scope}}` with the value "This policy applies to \{\{Legal\_Entities}}."

    If you use `{{Company_Scope}}` in your policy template, the generated document renders:

    "This policy applies to \{\{Legal\_Entities}}"

    instead of the expected:

    "This policy applies to Acme Ltd."

    Workaround: Use both variables directly in the policy template instead of nesting one inside the other. For example, add `{{Company_Scope}} {{Legal_Entities}}` directly in the template content where needed. This produces the same output without requiring nested variable resolution.
  </Expandable>
</ExpandableGroup>

